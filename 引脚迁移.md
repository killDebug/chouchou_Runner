# 接线总表（对着焊）

主机：**ESP32-S3 N8R8**  
开关 A / B：仍是 **ESP32-C3 SuperMini**（换同款板，引脚不变）

断电再拔插。灯带大电流 5V 走电源粗线，**所有 GND 必须共地**。

---

## 一、主机 ESP32-S3：谁接到哪一脚

| 外设 | 外设这一头 | 接到 S3 | 另一头 / 电源 |
|------|------------|---------|----------------|
| WS2812B 灯带 | DIN（控制线） | **GPIO2** | 灯带 **5V → 电源 5V**，灯带 **GND → 电源 GND**（与芯片共地） |
| OLED | SDA | **GPIO4** | |
| OLED | SCL | **GPIO5** | |
| OLED | VCC | — | **电源 5V**（和芯片、OLED 共 5V） |
| OLED | GND | — | **电源 GND** |
| SET / 功能键 | 按钮一脚 | **GPIO6** | 按钮另一脚 → **GND** |
| 加速键 | 按钮一脚 | **GPIO7** | 另一脚 → **GND** |
| 减速键 | 按钮一脚 | **GPIO8** | 另一脚 → **GND** |
| 压电蜂鸣器 | 信号脚 | **GPIO9** | 另一脚 → **GND** |
| MAX98357 | BCLK | **GPIO10** | |
| MAX98357 | LRC / LRCLK / WS | **GPIO11** | |
| MAX98357 | DIN | **GPIO12** | |
| MAX98357 | Vin | — | **电源 5V** |
| MAX98357 | GND | — | **电源 GND** |
| MAX98357 | GAIN | — | **悬空**（默认约 **9dB**；要更大声可直接接 GND→12dB） |
| MAX98357 | SD / SD_MODE | — | **不要悬空**：芯片内部约 **100k 下拉**，悬空≈关机无声。接 **Vin（5V）** 开左声道；或模块板上已有上拉则线材可不接 |
| 7030 喇叭 | SPK+ / SPK− | **不接 S3** | 只接功放 **+ / −**，禁止接 GND |
| 芯片供电 | 5V、GND | 板子 5V / GND | 同一电源 5V / GND |

**记法（信号脚连续）：GPIO2 灯带；4～8 人机；9 蜂鸣器；10～12 功放。**

不要占用：**GPIO0**（BOOT）、**GPIO1 / GPIO3**（CH343 串口）、**GPIO19 / GPIO20**（USB）、**GPIO33–37**（PSRAM）。

三键只接 GPIO 和 GND，**不要接到 5V**（固件内部上拉）。

先不接功放也能跑：灯带 + OLED + 三键 + 蜂鸣器。10/11/12 等上喇叭再接。

---

## 二、开关 A / B：ESP32-C3 SuperMini 引脚对应

板子：**ESP32-C3 SuperMini**（A、B 换同款板即可）。  
**硬件焊法完全一样**，差别只在烧哪个工程（A 发包 `"A"` / `PA`，B 发包 `"B"` / `PB`）。

角色（游戏语音）：**开关 A = 闪闪 `ss`**，**开关 B = 亮亮 `ll`**。

### 引脚一览（A、B 共用）

| 外设 | C3 引脚 | 固件宏 | 另一头 / 电源 |
|------|---------|--------|----------------|
| 游戏按钮 | **GPIO2** | `BUTTON_GPIO` | 另一脚 → **GND**（内部上拉，按下为 LOW） |
| 中间白色指示灯 | **GPIO3** | `WHITE_LED_GPIO` | 负极 → **GND** |
| 灯环 12 颗 WS281x DIN | **GPIO4** | `LED_PIN` | 灯环 **VCC → 5V**，**GND → 共地** |
| 板载蓝灯 CD2 | **GPIO8** | `BOARD_POWER_LED_GPIO` | 板上自带；LOW=亮，HIGH=灭；红灯 CD1 建议焊掉省电 |
| 芯片供电 | **5V / GND** | — | 与按钮、灯环 **共地**；灯环 5V 可与板子同一 5V |

**不要用 GPIO0**（Boot 键）。

### 记法

```
GPIO2  按钮
GPIO3  白灯
GPIO4  灯环 DIN
GPIO8  板载蓝灯
```

### 烧哪个工程

| 开关 | 工程目录 | 发出身份 | 语音角色 |
|------|----------|----------|----------|
| **A** | `chouchouswitchA` | `"A"` / `PA` | 闪闪 `ss` |
| **B** | `chouchouswitchB` | `"B"` / `PB` | 亮亮 `ll` |

### 换板步骤

1. 断电，拆掉坏的 SuperMini。  
2. 新板按上表焊：按钮 GPIO2、白灯 GPIO3、灯环 DIN GPIO4，电源 5V/GND。  
3. USB 烧固件（一次只插一块）：
   - 开关 A：`cd chouchouswitchA && make upload`
   - 开关 B：`cd chouchouswitchB && make upload`
4. 若串口不是 `platformio.ini` 里的口，先 `pio device list`，改 `upload_port` 再烧。

---

## 三、主机插头备忘（可选）

表一：**1× XH-7**（OLED+三键 GPIO4–8）+ **1× XH-2**（灯带 GPIO2）+ 可选蜂鸣器 XH-2（GPIO9）。  
表二再加：**1× XH-7**（功放 GPIO10–12）+ **1× XH-2**（喇叭只接功放）。

### XH-7 人机口

| Pin | 信号 | S3 |
|-----|------|-----|
| 1 | GND | GND |
| 2 | 5Vin | OLED VCC |
| 3 | SDA | GPIO4 |
| 4 | SCL | GPIO5 |
| 5 | SET | GPIO6 |
| 6 | 加速 | GPIO7 |
| 7 | 减速 | GPIO8 |

### XH-7 功放口

| Pin | 信号 | 接法 |
|-----|------|------|
| 1 | Vin | 电源 5V |
| 2 | GND | 电源 GND |
| 3 | SD | **接 Vin**（与 pin1 短接即可开功放）；仅当模块板上已有 SD→Vin 上拉时可悬空 |
| 4 | GAIN | 悬空（9dB） |
| 5 | DIN | GPIO12 |
| 6 | BCLK | GPIO10 |
| 7 | LRC | GPIO11 |

**SD / GAIN 对照（MAX98357A 手册）：**

| 脚 | 悬空行不行 | 实际效果 |
|----|------------|----------|
| GAIN | 可以 | 约 9dB；接 GND=12dB，接 Vin=6dB |
| SD | **不行**（裸片/无上拉模块） | 内部下拉 → 关机；必须拉高才出声 |

常见成品小板若 SD 已通过 100k～1M 接到 Vin，则插头上 SD 线悬空也行；若只有焊盘、板上无上拉，SD 必须接到 Vin，否则喇叭无声或只有干扰哒哒。

---

## 四、固件宏（主机 S3）

| 宏 | S3 |
|----|-----|
| `LED_PIN` | 2 |
| `OLED_SDA` / `OLED_SCL` | 4 / 5 |
| `FORCE_OFFLINE_SWITCH_GPIO` | 6 |
| `GAME_SPEED_UP_GPIO` / `GAME_SPEED_DOWN_GPIO` | 7 / 8 |
| `BUZZER_PIN` | 9 |
| I2S BCLK / LRC / DIN | 10 / 11 / 12 |

## 五、固件宏（开关 A / B，C3，两边相同）

| 宏 | C3 |
|----|-----|
| `BUTTON_GPIO` | 2 |
| `WHITE_LED_GPIO` | 3 |
| `LED_PIN`（灯环） | 4 |
| `BOARD_POWER_LED_GPIO`（板载蓝灯） | 8 |
